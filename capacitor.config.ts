import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto_auth_storage_003',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
